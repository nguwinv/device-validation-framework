# device-validation-framework
A Flask web app that simulates a company-style device monitoring dashboard (device sync status, sensor readings, session logs) Selenium tests that automatically validate everything on that dashboard
